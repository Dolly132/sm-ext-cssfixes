name: Build AlliedModders Extension

on:
  push:
    branches:
      - master
      - main
      - test-x64
    tags:
      - "*"
  pull_request:
    branches:
      - master
      - main

jobs:
  build-release-extension:
    uses: Dolly132/ci-workflows/.github/workflows/shared_build_release_am_extension.yml@main
    with:
      sdks: "css"
      os-list: '["ubuntu-22.04"]'
